{
  "devDependencies": {
    "electron": "^14.0.0-beta.25",
    "esbuild": "^0.17.2"
  },
  "scripts": {
    "build": "esbuild ./src/index.js --sourcemap=inline --format=cjs --platform=node --external:electron --external:path --external:fs --bundle --outfile=./build.js"
  }
}